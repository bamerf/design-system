import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import Input from "./Input";

<Meta title="Design System|Input" component={Input} />

# Input

Input fields are used for the user to type their value as text, number, password or date.

### Default Input

<Preview withToolbar>
	<Story name="Default">
		<Input />
	</Story>
</Preview>

Labelled Input

<Preview withToolbar>
	<Story name="Labelled">
		<Input label="Labelled Input" />
	</Story>
</Preview>

### Sizes

<Preview withToolbar>
	<Story name="Large">
		<Input label="Large Input" size="large" />
	</Story>
	<Story name="Small">
		<Input label="Small Input" size="small" />
	</Story>
</Preview>

### Status

<Preview withToolbar>
	<Story name="Error Small">
		<Input label="Error Small" size="small" status="error" />
	</Story>
	<Story name="Error Medium">
		<Input
			label="Error Medium"
			status="error"
			helperText="Medium field helper text"
		/>
	</Story>
	<Story name="Error Large">
		<Input
			label="Error Large"
			size="large"
			status="error"
			helperText="Large field helper text"
		/>
	</Story>
</Preview>

### Disabled

<Preview withToolbar>
	<Story name="Disabled">
		<Input label="Disabled" status="disabled" value="External Value" />
	</Story>
</Preview>
