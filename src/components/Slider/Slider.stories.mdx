import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import Slider from "./Slider";

<Meta title="Design System|Slider" component={Slider} />

# Slider

Sliders are used to select a value in a ranges of values.

### Default Slider

<Preview withToolbar>
	<Story name="Default">
		<Slider min={0} max={100} value={0} />
	</Story>
</Preview>

### Labelled

<Preview withToolbar>
	<Story name="Labelled">
		<Slider label="Labelled Slider" min={0} max={100} value={0} />
	</Story>
</Preview>

### Sizes

<Preview withToolbar>
	<Story name="Large">
		<Slider label="Large Slider" min={0} max={100} value={50} size="large" />
	</Story>
</Preview>

### Disabled

<Preview withToolbar>
	<Story name="Disabled">
		<Slider label="Slider" min={0} max={100} value={0} status="disabled" />
	</Story>
	<Story name="Disabled Selected">
		<Slider label="Slider" min={0} max={100} value={50} status="disabled" />
	</Story>
</Preview>
